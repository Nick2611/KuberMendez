apiVersion: 123
kind: Deployment
metadata:
  name: Nico
spec:
  selector:
    matchLabels:
      app: nico-nginx
      nico: yo
  template:
    metadata:
      name: nico-nginx
      labels:
        app: nico-nginx
    spec:
      replicas: 3
      containers:
        - name: nginx
          image: pufd23
          ports:
            - containerPort: 80
              hostPort: 8080